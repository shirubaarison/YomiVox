{
  "name": "yomivox",
  "version": "1.0.1",
  "description": "Generates Japanese audio via VOICEVOX and attaches it to your latest Anki card.",
  "main": "background.js",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -a public/. dist/",
    "watch": "tsc --watch",
    "package": "npm run build && rm -f yomivox.xpi && cd dist && zip -r ../yomivox.xpi . && cd .."
  },
  "keywords": [],
  "author": "shirubaarison",
  "license": "AGPL-3.0",
  "type": "commonjs",
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "typescript": "^6.0.3"
  }
}
