{
  "name": "yomivox",
  "version": "1.0.0",
  "description": "Read Japanese text using VOICEVOX and add audio to your Anki cards.",
  "main": "background.js",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -a public/. dist/",
    "watch": "tsc --watch"
  },
  "keywords": [],
  "author": "shirubaarison",
  "license": "AGPL-3.0",
  "type": "commonjs",
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "typescript": "^6.0.3"
  }
}
